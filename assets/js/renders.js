const lightbox=document.querySelector('#lightbox');const lightboxImage=lightbox.querySelector('img');document.querySelectorAll('.image-button').forEach(button=>button.addEventListener('click',()=>{lightboxImage.src=button.dataset.full;lightbox.querySelector('p').textContent=button.closest('article')?.querySelector('h3')?.textContent||'Render ampliado';lightbox.showModal()}));
const comparison=document.querySelector('#comparison');document.querySelectorAll('.compare').forEach(button=>button.addEventListener('click',()=>{comparison.querySelector('.before').src=button.dataset.original;comparison.querySelector('.after').src=button.dataset.render;comparison.showModal()}));
document.querySelectorAll('dialog .close').forEach(button=>button.addEventListener('click',()=>button.closest('dialog').close()));document.querySelectorAll('dialog').forEach(dialog=>dialog.addEventListener('click',event=>{if(event.target===dialog)dialog.close()}));document.addEventListener('keydown',event=>{if(event.key==='Escape')document.querySelectorAll('dialog[open]').forEach(dialog=>dialog.close())});
document.querySelectorAll('.filters button').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.filters button').forEach(item=>item.classList.remove('active'));button.classList.add('active');const filter=button.dataset.filter;document.querySelectorAll('.gallery .render-card').forEach(card=>card.classList.toggle('hidden',filter!=='all'&&!card.dataset.category.split(' ').includes(filter)))}));
